{
    "actorOutputSchemaVersion": 1,
    "title": "Cheapest Plane Tickets Output",
    "properties": {
        "flightResults": {
            "type": "string",
            "title": "Flight Results (Overview)",
            "template": "{{links.apiDefaultDatasetUrl}}/items?view=overview"
        },
        "allData": {
            "type": "string",
            "title": "Complete Dataset (JSON)",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}
